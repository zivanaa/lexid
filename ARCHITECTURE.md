# Architecture

> Auto-generated by `scripts/gen_architecture.py` (pre-commit hook). Do NOT edit by hand.
> Nodes = project modules, edges = imports (incl. lazy/deferred). GitHub renders this inline.

```mermaid
graph LR
  subgraph evals
    evals_calibrate_judge["calibrate_judge"]
    evals_gate_check["gate_check"]
    evals_make_review_sheet["make_review_sheet"]
    evals_run_generation["run_generation"]
    evals_run_retrieval["run_retrieval"]
  end
  subgraph finetuning
    finetuning_dataset_anonymize["dataset.anonymize"]
  end
  subgraph ingestion
    ingestion_chunk["chunk"]
    ingestion_embed["embed"]
    ingestion_index["index"]
    ingestion_parse["parse"]
    ingestion_run["run"]
  end
  subgraph prompts
    prompts_loader["loader"]
  end
  subgraph rag
    rag___main__["__main__"]
    rag_generate["generate"]
    rag_hybrid["hybrid"]
    rag_llm_client["llm_client"]
    rag_pipeline["pipeline"]
    rag_rerank["rerank"]
    rag_retrieve["retrieve"]
  end
  subgraph root
    config["config"]
  end
  subgraph scripts
    scripts_gen_architecture["gen_architecture"]
    scripts_healthcheck["healthcheck"]
  end
  evals_make_review_sheet --> evals_run_retrieval
  evals_make_review_sheet --> ingestion_chunk
  evals_make_review_sheet --> ingestion_parse
  evals_make_review_sheet --> ingestion_run
  evals_run_generation --> evals_run_retrieval
  evals_run_generation --> prompts_loader
  evals_run_generation --> rag_generate
  evals_run_generation --> rag_llm_client
  evals_run_generation --> rag_pipeline
  evals_run_retrieval --> rag_hybrid
  evals_run_retrieval --> rag_rerank
  evals_run_retrieval --> rag_retrieve
  ingestion_chunk --> ingestion_parse
  ingestion_embed --> config
  ingestion_index --> config
  ingestion_index --> ingestion_chunk
  ingestion_run --> ingestion_chunk
  ingestion_run --> ingestion_embed
  ingestion_run --> ingestion_index
  ingestion_run --> ingestion_parse
  rag___main__ --> rag_pipeline
  rag_generate --> prompts_loader
  rag_generate --> rag_llm_client
  rag_generate --> rag_retrieve
  rag_hybrid --> ingestion_index
  rag_hybrid --> rag_retrieve
  rag_llm_client --> config
  rag_pipeline --> rag_generate
  rag_pipeline --> rag_hybrid
  rag_pipeline --> rag_rerank
  rag_pipeline --> rag_retrieve
  rag_rerank --> config
  rag_rerank --> rag_hybrid
  rag_rerank --> rag_retrieve
  rag_retrieve --> ingestion_embed
  rag_retrieve --> ingestion_index
  scripts_healthcheck --> config
```
